<h2>FoosController#new</h2>
<%= labelled_form_for :foo, @foo,
  :url => project_foos_path(@project) do |f| %>
  <%= render :partial => 'foos/form', :locals => {:form => f} %>
  <%= f.submit l(:button_create) %>
<% end %>
